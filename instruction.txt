Да, в этих фрагментах кода все требования задания выполнены:

1. В services/contact_book.py  
   - Метод `get_birthdays_in(days)` реализован: рассчитывает ближайший день рождения (в текущем или следующем году), проверяет, попадает ли он в интервал `0…days`, и при совпадении:
     - если это суббота, сдвигает дату на понедельник (+2 дня);
     - если это воскресенье, сдвигает на понедельник (+1 день);
     - возвращает список кортежей `(имя, скорректированная_дата)`.

2. В cli/command_handler.py  
   - Добавлен блок `elif cmd.startswith("show birthdays")`:  
     - разбирает команду, проверяя, что после неё идёт неотрицательное целое;  
     - вызывает `contact_book.get_birthdays_in(days)`;  
     - выводит “No birthdays…” при пустом списке или перечисляет `name: YYYY-MM-DD`.

Таким образом:
- метод реализован там, где нужно;
- команда в CLI добавлена и проверяет валидность `days`;
- при выводе учитываются выходные, смещение на понедельник.
